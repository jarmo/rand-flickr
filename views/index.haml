#container
  .pure-g.main
    .pure-u-1-1
      %p.error.pure-u-3-6
        = @error      
      %h1
        %form.pure-form(action="/photo" method="GET")
          rand(
          %input.pure-input-1-5(name="user" type="text" placeholder="flickr user")
          )
          %button.pure-button
            %i.fa.fa-flickr
            %span show
    .pure-u-5-6.predefined
      %a(href="/photo/jarm0") or just view random user images
